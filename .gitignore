# generated artifacts (rebuild with: python build.py)
dist/
__pycache__/
*.pyc
cache/search_index/

# NOT ignored on purpose (they are records, not build products):
#   cache/     — verbatim fetched sources; the validator hash-checks these
#   snapshot/  — the committed knowledge-state baseline
#   events/    — historical change records
#   audit/     — human decision log
#   candidates/— pending discovery inlet
