"""Source adapters — plugin interface for external knowledge sources.

Each adapter knows how to search, fetch, and extract evidence from one source.
The pipeline treats them interchangeably through the SourceAdapter ABC.
"""
